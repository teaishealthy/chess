[project]
name = "chess"
version = "0.1.0"
description = "A really bad chess engine implemented in Python."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "pytest-benchmark>=5.2.1",
    "py-spy>=0.4.1",
]
